#!/bin/bash

export PATH=$PATH:/usr/local/share/python

dir=`dirname $1`
cd $dir
source=`basename $1 | sed -e 's/_flymake//'`
file=`git ls-files $source 2> /dev/null`
# only check if this file is in git
if [ -e "$file" ]; then
    flake8 "$1"
fi
